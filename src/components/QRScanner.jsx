// Deprecated. The app now DISPLAYS a QR code on the main screen
// (see VoteQRCode.jsx) which viewers scan with their phone's native camera
// to open the /vote page. This in-app camera scanner is no longer used.
export default function QRScanner() {
  return null;
}
