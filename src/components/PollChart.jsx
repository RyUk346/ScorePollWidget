// Deprecated. The live bars are now rendered inline in MainScreen.jsx (the
// compact widget) and per-match in MatchVoteCard.jsx. Kept as a no-op so any
// stray import won't break the build.
export default function PollChart() {
  return null;
}
